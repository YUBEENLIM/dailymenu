spring:
  docker:
    compose:
      enabled: false # 변경된 부분: 테스트에서 docker-compose 자동 실행 방지

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.testcontainers: INFO
    org.springframework.test.context: INFO